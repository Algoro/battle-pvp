{
  "name": "battlecity-qa",
  "version": "0.1.0",
  "description": "Автотесты: ASM-патчи (headless-эмуляция), netcode (джиттер/потери), sync-test режим, нагрузка комнат, e2e Playwright.",
  "type": "module",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "test": "node ../scripts/prepare.mjs && node --disable-warning=ExperimentalWarning --test tests/*.test.ts",
    "e2e": "node ../scripts/prepare.mjs && playwright test",
    "e2e:online": "node ../scripts/prepare.mjs && playwright test -c playwright.online.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
